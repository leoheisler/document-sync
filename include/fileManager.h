#ifndef FILEMANAGER_H
#define FILEMANAGER_H
#include <string>
#include <vector>
#include <sstream>

#include "clientList.h"
#include "packet.h"

class fileManager
{
    private: 

    public:
        fileManager(/* args */);
};  
#endif